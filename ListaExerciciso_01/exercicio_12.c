#include <stdio.h>

/*
    Escreva um programa que leia da entrada padrão os valores 
    reais dos coeficientes a, b e c, de um polinômio de segundo 
    grau do tipo p(x)=ax2+bx+c, e imprima:
    a) Se o polinômio tem uma, duas ou nenhuma raiz real (lembre 
    das suas aulas de segundo grau qual a condição que define o 
    número de raízes de um polinômio);
    b) Se tiver raízes (uma ou duas) imprima o valor das mesmas. 
    No exercício anterior você deve ter descoberto como utilizar a 
    função sqrt() para calcular raízes quadradas;
    c) Leia da entrada padrão um valor para x e imprima o valor de 
    p(x);
*/

int main(void)
{
    return 0;
}
#include <stdio.h>

/*
    Escreva um programa que leia da entrada padrão o tamanho 
    dos catetos de um triângulo retângulo e imprima:
    a) O tamanho do quadrado da hipotenusa;
    b) O arquivo de cabeçalho math.h inclui a função sqrt() que 
    retorna a raiz quadrada do parâmetro de entrada. 
    c) O valor do perímetro do triângulo;
    d) O valor da área do referido triângulo;
*/

int main(void)
{
    return 0;
}
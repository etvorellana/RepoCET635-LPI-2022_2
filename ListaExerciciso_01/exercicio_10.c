#include <stdio.h>

/*
    Escreva um programa que leia dois pares de valores (x1, y1) 
    e (x2, y2) de pontos do plano e informe a equação da função 
    do tipo f(x)= ax + b que passa por estes pontos.
*/

int main(void)
{
    return 0;
}
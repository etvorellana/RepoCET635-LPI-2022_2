#include <stdio.h>

/*
    Escreva um programa que:
    a) Declare três variáveis de tipo char, vamos chamar de 
    ch1, ch2 e ch3  mas você pode escolher outro identificador 
    se achar mais apropriado;
    c) Peça ao usuário para digitar um caractere, obtenha o 
    mesmo da entrada padrão, usando scanf, e atribua o valor 
    digitado à variável ch1;
    d) Verifique, utilizando o operador condicional (?) se se 
    trata de:
        * um dígito;
        * um caractere maiúsculo;
        * um caractere minúsculo;
        * outro tipo de caractere;
    e) Atribua à variável ch2 o caractere 78 e identifique é 
    imprima na tela, utilizando printf, o caractere em formato 
    numérico decimal, octal, hexadecimal e como caractere;
    f) Dado que o caractere ch2 é uma letra maiúscula, atribua 
    à variável ch3 o caractere que corresponde à mesma letra 
    minúscula (não pode procurar na tabela ASCII); 
    g) Imprima ch3 na tela, utilizando printf, em formato numérico  
    decimal, octal, hexadecimal e como caractere;
*/

int main(void)
{
    return 0;
}
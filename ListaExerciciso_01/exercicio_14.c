#include <stdio.h>

/*
    Uma circunferência de raio 1.0 é composta pelos pontos 
    (x, y) que satisfazem a condição x2+ y2= 1.0. Escreva 
    um programa que leia as coordenadas (x, y) de um ponto 
    pelo teclado e determine se o ponto está dentro ou fora 
    da circunferência.
*/

int main(void)
{
    return 0;
}
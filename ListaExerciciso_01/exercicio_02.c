#include <stdio.h>

/*
    Escreva um programa, usando quando necessário o operador 
    (?), que:
    a) Declare três variáveis de tipo int, vamos chamar de a, 
    b e c mas você pode escolher outro identificador se achar 
    mais apropriado; 
    b) Peça ao usuário para digitar dois números inteiros, 
    obtenha-os da entrada padrão, usando scanf,  e atribua os 
    valores digitados às variáveis a e b respectivamente;
    c) Atribua à variável c a somas das variáveis a e b, imprima 
    o resultado na tela em formato octal; 
    d) Atribua à variável c o produto das variáveis a e b, imprima 
    o resultado na tela em formato hexadecimal; 
    e) Atribua à variável c o módulo (valor absoluto) da diferença 
    entre as variáveis a e b, imprima o resultado na tela; 
    (valor absoluto: se c < 0 imprimir -c) 
    f) Atribua à variável c o quociente entre variáveis a e b, 
    imprima o resultado na tela; (se b = 0 a divisão não é possível e 
    um aviso deve ser apresentado ao usuário) 
    g) Se a divisão anterior for possível, determinar se a é divisível 
    de forma exata por b, mostrar o resultado na tela;
*/

int main(void)
{
    return 0;
}
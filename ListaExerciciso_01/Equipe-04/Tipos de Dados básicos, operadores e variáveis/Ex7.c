#include <stdio.h>
#include <stdlib.h>

int main()
{
  int n;

     printf("entre com um valor inteiro");
     scanf("%d", &n);

    (n < 0) ? printf("o valor da distancia até zero eh: %d", n = -1*((n*(n+1)/2)-n)) : printf("o valor da distancia ate zero eh: %d", n = n*(n+1)/2);


    return 0;

}
